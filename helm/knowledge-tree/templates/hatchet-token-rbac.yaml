{{- if and .Values.hatchet.tokenJob.enabled (not .Values.secrets.hatchetClientToken) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "knowledge-tree.fullname" . }}-hatchet-token
  labels:
    {{- include "knowledge-tree.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "knowledge-tree.fullname" . }}-hatchet-token
  labels:
    {{- include "knowledge-tree.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: [{{ include "knowledge-tree.secretName" . | quote }}]
    verbs: ["get", "patch"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "knowledge-tree.fullname" . }}-hatchet-token
  labels:
    {{- include "knowledge-tree.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "knowledge-tree.fullname" . }}-hatchet-token
    namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "knowledge-tree.fullname" . }}-hatchet-token
{{- end }}
