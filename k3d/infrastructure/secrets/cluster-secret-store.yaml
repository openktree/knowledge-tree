---
# Centralized ClusterSecretStore for all applications
# This allows ExternalSecrets to reference password generators

apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: password-store
spec:
  # This store is accessible from the share-app namespace only
  conditions:
    - namespaces:
        - share-app
